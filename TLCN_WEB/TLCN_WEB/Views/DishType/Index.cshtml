@model IEnumerable<TLCN_WEB.Models.DishType>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DishType_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DishyTypeName)
        </th>
  
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DishType_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DishyTypeName)
            </td>
           

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DishType_ID }) |
                @Html.ActionLink("Detail", "Detail", new { id = item.DishType_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DishType_ID })
            </td>
        </tr>
    }

</table>

