@model IEnumerable<TLCN_WEB.Models.BusinessType>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BusinessTypeID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BusinessTypeName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BusinessTypeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BusinessTypeName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.BusinessTypeID }) |
                @Html.ActionLink("Detail", "Detail", new { id = item.BusinessTypeID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.BusinessTypeID })
            </td>
        </tr>
    }

</table>

